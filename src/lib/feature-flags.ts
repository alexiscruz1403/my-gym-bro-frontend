/**
 * Los pagos de membresía premium están suspendidos: todos los usuarios registrados
 * reciben premium como promoción. Cambiar a `true` para reactivar el checkout de
 * Mercado Pago, la renovación automática y toda la UI de cobros.
 */
export const MEMBERSHIP_PAYMENTS_ENABLED: boolean = false;
